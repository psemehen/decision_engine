<% if target_object.errors.any? %>
  <div id="error_explanation" class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative">
    <h2 class="font-semibold mb-2"><%= pluralize(target_object.errors.count, "error") %> prohibited this from being saved:</h2>
    <ul class="list-disc ml-5">
      <% target_object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
