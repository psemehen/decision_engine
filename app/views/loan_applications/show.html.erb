<% content_for :title, "Loan Application Details" %>

<div class="mb-4">
  <% if @loan_application.present? %>
    <div class="bg-white p-4 shadow rounded">
      <p class="font-bold">Personal Code: <%= @loan_application.personal_code %></p>
      <p class="font-bold">Loan Amount: <%= @loan_application.approved_amount %> €</p>
      <p class="font-bold">Loan Period: <%= @loan_application.loan_period %> months</p>
    </div>
  <% end %>
</div>

<div class="mb-4 flex  space-x-4">
  <%= link_to "< Back", loan_applications_path, class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded' %>
  <%= button_to "Delete", loan_application_path(@loan_application), method: :delete, class: 'bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded' %>
</div>
